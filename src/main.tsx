import React from 'react';
import ReactDOM from 'react-dom/client';
import { AICustomerServicePlatform } from '../components/customer-service/AICustomerServicePlatform';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AICustomerServicePlatform />
  </React.StrictMode>,
);
